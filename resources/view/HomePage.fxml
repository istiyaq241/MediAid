<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="mediAid.controller.HomePageController"
            prefWidth="1100" prefHeight="680"
            style="-fx-background-color: linear-gradient(to bottom, #c7eef7, #4db6f4);">

    <!-- Top bar: disclaimer + Reminders button (snapshot 2 look) -->
    <top>
        <BorderPane>
            <left>
                <HBox alignment="CENTER_LEFT" spacing="8" style="-fx-padding: 8 12 0 12;">
                    <!-- (Optional) menu icon was decorative in snapshot 2; removed action to avoid errors -->
                    <Button text="☰" disable="true" />
                    <Label text="This is not a substitute for professional medical advice."
                           style="-fx-text-fill: #c62828; -fx-font-size: 14px; -fx-font-weight: bold;"/>
                </HBox>
            </left>
            <right>
                <HBox alignment="CENTER_RIGHT" spacing="8" style="-fx-padding: 8 12 0 12;">
                    <Button text="🔔 Reminders" onAction="#goToReminder"/>
                </HBox>
            </right>
        </BorderPane>
    </top>

    <!-- Center: two large buttons -->
    <center>
        <VBox alignment="CENTER" spacing="24">
            <HBox alignment="CENTER" spacing="28">
                <Button text="🆘 Accident Help"
                        onAction="#goToAccident"
                        minWidth="220" minHeight="56"
                        style="-fx-background-color:#0d6efd; -fx-text-fill:white; -fx-background-radius:12;"/>
                <Button text="🩺 Disease Help"
                        onAction="#goToDisease"
                        minWidth="220" minHeight="56"
                        style="-fx-background-color:#0d6efd; -fx-text-fill:white; -fx-background-radius:12;"/>
            </HBox>
        </VBox>
    </center>

    <!-- (Optional) light watermark background you had before -->
    <bottom>
        <HBox alignment="CENTER">
            <Label text="" />
        </HBox>
    </bottom>
</BorderPane>
